#include <iostream>

int main() {
    int start = 0;
    int text_len = 100;
    int direction = 1;
    int i;

    for ( i = start; i >= 0 AND i < text_len; i += direction ) {
        // placeholder body
        std::cout << "i = " << i << std::endl;
    }

    return 0;
}